<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>전시 적층(Layers) - 포트폴리오</title>
    <style>
        :root {
            --bg-color: #0f0f0f;
            --text-color: #ffffff;
            --accent-color: #555;
        }

        body, html {
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Pretendard', sans-serif;
            overflow-x: hidden;
        }

        /* 스크롤 가이드 */
        .scroll-container {
            height: 500vh; /* 스크롤 길이를 길게 설정하여 적층 단계 표현 */
            position: relative;
        }

        .sticky-wrapper {
            position: sticky;
            top: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* 레이어 기본 스타일 */
        .layer {
            position: absolute;
            width: 80%;
            max-width: 900px;
            transition: all 0.8s cubic-bezier(0.2, 0.8, 0.2, 1);
            opacity: 0;
            transform: scale(1.1) translateZ(0);
            text-align: center;
        }

        .layer.active {
            opacity: 1;
            transform: scale(1) translateZ(0);
        }

        .layer.past {
            opacity: 0.3;
            transform: scale(0.9) translateY(-50px);
            filter: blur(5px);
        }

        h1 { font-size: 4rem; letter-spacing: 0.5rem; margin-bottom: 10px; }
        h2 { font-size: 1.5rem; font-weight: 300; color: #888; }
        .work-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 40px;
        }

        .work-item {
            border: 1px solid var(--accent-color);
            padding: 40px 20px;
            background: rgba(255, 255, 255, 0.05);
            transition: 0.3s;
        }

        .work-item:hover { background: rgba(255, 255, 255, 0.1); }

        .performance-box {
            border: 2px solid #fff;
            padding: 60px;
            background: rgba(255,255,255,0.1);
            animation: pulse 3s infinite;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(255,255,255,0.4); }
            70% { box-shadow: 0 0 0 20px rgba(255,255,255,0); }
            100% { box-shadow: 0 0 0 0 rgba(255,255,255,0); }
        }

        #nav {
            position: fixed;
            right: 30px;
            top: 50%;
            transform: translateY(-50%);
            z-index: 100;
        }

        #nav div {
            width: 10px;
            height: 10px;
            border: 1px solid #fff;
            margin: 15px 0;
            border-radius: 50%;
            transition: 0.3s;
        }

        #nav div.on { background: #fff; transform: scale(1.5); }

    </style>
</head>
<body>

    <div id="nav">
        <div class="dot on"></div>
        <div class="dot"></div>
        <div class="dot"></div>
        <div class="dot"></div>
        <div class="dot"></div>
    </div>

    <div class="scroll-container">
        <div class="sticky-wrapper">
            
            <div class="layer active" id="l0">
                <h1>적층</h1>
                <h2>Layers of Existence</h2>
                <p style="margin-top: 50px; color: #666;">SCROLL TO LAYER</p>
            </div>

            <div class="layer" id="l1">
                <h2>LAYER 01: 개별의 층위</h2>
                <div class="work-grid">
                    <div class="work-item">작가 A 개인작</div>
                    <div class="work-item">작가 B 개인작</div>
                    <div class="work-item">작가 C 개인작</div>
                </div>
                <p style="margin-top: 30px;">각자의 고유한 시간과 서사가 쌓이는 단계</p>
            </div>

            <div class="layer" id="l2">
                <h2>LAYER 02: 교차된 층위</h2>
                <div class="work-grid">
                    <div class="work-item">A & B 협업</div>
                    <div class="work-item">B & C 협업</div>
                    <div class="work-item">C & A 협업</div>
                </div>
                <p style="margin-top: 30px;">두 개의 세계가 만나 겹치고 확장되는 지점</p>
            </div>

            <div class="layer" id="l3">
                <h2>LAYER 03: 공동의 층위</h2>
                <div class="performance-box">
                    <h3>퍼포먼스: [적층된 행위]</h3>
                    <p>3인의 작가가 함께 완성한 퍼포먼스의 흔적이 공간을 채웁니다.</p>
                </div>
                <p style="margin-top: 30px;">하나의 거대한 서사로 합쳐진 완전한 층위</p>
            </div>

            <div class="layer" id="l4">
                <h2>EPILOGUE: 아카이빙</h2>
                <p>전시 기획, 운영 매니징, 기록의 과정</p>
                <div style="margin-top: 20px; font-size: 0.9rem; color: #888; line-height: 1.8;">
                    커뮤니케이션 / 홍보 / CS / 국가 지원 사업 보고서 작성<br>
                    프로그램 운영 및 인력 관리
                </div>
            </div>

        </div>
    </div>

    <script>
        const layers = document.querySelectorAll('.layer');
        const dots = document.querySelectorAll('.dot');
        
        window.addEventListener('scroll', () => {
            const scrollPercent = window.scrollY / (document.body.offsetHeight - window.innerHeight);
            const step = Math.floor(scrollPercent * 5); // 0~4 단계

            layers.forEach((layer, index) => {
                layer.classList.remove('active', 'past');
                dots[index].classList.remove('on');

                if (index === step) {
                    layer.classList.add('active');
                    dots[index].classList.add('on');
                } else if (index < step) {
                    layer.classList.add('past');
                }
            });
        });
    </script>
</body>
</html>
